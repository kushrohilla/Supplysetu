export type PushNotificationItem = {
  id: string;
  title: string;
  body: string;
  createdAt: string;
};

export type InactivityReminder = {
  title: string;
  body: string;
};

export type SchemeAlert = {
  title: string;
  body: string;
};
